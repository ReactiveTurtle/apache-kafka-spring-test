server:
  port: 9297

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: file://D:/Projects/IdeaProjects/carsarchtecture
          clone-on-start: true
